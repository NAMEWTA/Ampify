{
  "name": "copy-path-line",
  "displayName": "复制路径与行号",
  "description": "复制文件路径与行号的 VS Code 扩展。",
  "version": "1.0.0",
  "publisher": "NAMEWTA",
  "repository": {
    "type": "git",
    "url": ""
  },
  "icon": "icon.png",
  "extensionKind": [
    "workspace"
  ],
  "engines": {
    "vscode": "^1.83.0"
  },
  "keywords": [
    "copy",
    "file name",
    "line number",
    "function name",
    "clipboard"
  ],
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "file-line-reference.copy-relative-path-line",
        "title": "Copy Relative Path & Line (复制相对路径与行号)"
      },
      {
        "command": "file-line-reference.copy-absolute-path-line",
        "title": "Copy Absolute Path & Line (复制绝对路径与行号)"
      }
    ],
    "keybindings": [
      {
        "command": "file-line-reference.copy-relative-path-line",
        "key": "alt+y",
        "mac": "ctrl+y",
        "when": "editorTextFocus"
      },
      {
        "command": "file-line-reference.copy-absolute-path-line",
        "key": "alt+u",
        "mac": "ctrl+u",
        "when": "editorTextFocus"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "file-line-reference.copy-relative-path-line"
        },
        {
          "command": "file-line-reference.copy-absolute-path-line"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/vscode": "^1.83.0",
    "@types/node": "18.x",
    "@typescript-eslint/eslint-plugin": "^6.7.3",
    "@typescript-eslint/parser": "^6.7.3",
    "eslint": "^8.50.0",
    "typescript": "^5.2.2"
  }
}
